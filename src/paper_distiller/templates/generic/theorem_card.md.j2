# Theorem Card: {{ stem }}

Extract numbered assumptions, theorems, lemmas, propositions, corollaries,
algorithms, and equations.

Preserve numbering and mark uncertain formulas as `needs PDF check`.

## Distillation Profile

- Discipline: `{{ discipline }}`
- Output language: `{{ output_language }}`
- Math level: `{{ math_level }}`

{{ language_instruction }}

{{ discipline_instruction }}

{{ math_instruction }}

If `math_level` is `none` or the paper is non-mathematical, do not force
theorem extraction. Instead create an "Evidence / Claim Card" covering key
claims, data sources, methods, empirical findings, conceptual propositions, and
verification risks.

## Paper Text

```text
{{ paper_text }}
```
