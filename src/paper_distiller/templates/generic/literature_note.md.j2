# Literature Note: {{ stem }}

Summarize the paper for a reusable research knowledge base.

## Distillation Profile

- Preset: `{{ preset }}`
- Discipline: `{{ discipline }}`
- Audience: `{{ audience }}`
- Source language: `{{ source_language }}`
- Output language: `{{ output_language }}`
- Depth: `{{ depth }}`
- Math level: `{{ math_level }}`

{{ language_instruction }}

{{ discipline_instruction }}

{{ depth_instruction }}

{{ math_instruction }}

Rules:

- Do not invent facts not present in the paper text.
- Mark uncertain formulas and claims as `needs PDF check`.
- Every section must include `Reliability / Verification status: ...`.

Include:

{% if category == "C_background" %}
For `C_background`, keep the note short and include a clearly labeled
`Relevance score` with a brief justification.
{% endif %}

1. Metadata
2. Research question
3. Method
4. Evidence, data, or source material
5. Assumptions, theory, or conceptual framework when applicable
6. Main results
7. Limitations
8. Reusable insights and writing patterns

## Paper Text

```text
{{ paper_text }}
```
