# Theorem Comparison: {{ batch_id }}

Compare numbered results and rates. Cite source files and avoid forced
comparisons across incompatible settings.

## Distillation Profile

- Discipline: `{{ discipline }}`
- Output language: `{{ output_language }}`
- Math level: `{{ math_level }}`

{{ language_instruction }}

{{ discipline_instruction }}

{{ math_instruction }}

For non-mathematical batches, compare claim/evidence structures, study designs,
datasets, methods, or conceptual frameworks instead of theorem rates.

## Source Files

{% for path, content in source_files.items() %}
### {{ path }}

```markdown
{{ content }}
```

{% endfor %}
